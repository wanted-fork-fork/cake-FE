import UserMainTemplate from "@src/templates/UserMain.template";

export default {
  title: "template/User Main",
  component: UserMainTemplate,
};

const Template = () => <UserMainTemplate />;

export const UserMain = Template.bind({});
