import BottomNavigationComponent from "@src/components/organs/BottomNavigation.component";

export default {
  title: "organs/Bottom Navigation",
  component: BottomNavigationComponent,
};

const Template = () => <BottomNavigationComponent />;

export const BottomNavigation = Template.bind({});
